<?xml version="1.0" encoding="UTF-8"?>
<!--
    TODO:
         root="true" marks the root directory of the project (${project.rootDirectory}) -->
<project xmlns="http://maven.apache.org/POM/4.1.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.1.0 https://maven.apache.org/xsd/maven-4.1.0.xsd"
         root="true">
    <!--
    TODO:
        model version 4.1.0 (build POM only, the consumer POM stays 4.0.0) -->
    <modelVersion>4.1.0</modelVersion>

    <groupId>com.gepardec.maven-training</groupId>
    <artifactId>example-parent</artifactId>
    <!--
    TODO:
        full CI-friendly version support, no flatten-maven-plugin needed.
        Override with: mvn verify -Drevision=1.2.3 -->
    <version>${revision}</version>

    <packaging>pom</packaging>
    <name>example-parent</name>

    <!--
      TODO:
        <subprojects> replaces <modules>.
        (It could even be omitted completely: with packaging "pom" and no <subprojects>,
        Maven 4 discovers every direct subdirectory that contains a pom.xml.)
    -->
    <subprojects>
        <subproject>example-bom</subproject>
        <subproject>example-domain</subproject>
        <subproject>example-service</subproject>
        <subproject>example-webapp</subproject>
    </subprojects>

    <properties>
        <revision>1.0-SNAPSHOT</revision>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>17</maven.compiler.release>
        <junit.version>5.11.4</junit.version>
    </properties>

    <!--
    TODO:
        No dependencyManagement entries for our own subprojects:
        Maven 4 infers the version of dependencies on projects of the same reactor.
    -->
    <dependencyManagement>
        <dependencies>
            <!--
            TODO:
                Only import external BOMs. Importing a BOM from the same reactor
                (e.g. example-bom) triggers a warning in Maven 4. -->
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!--
    TODO:
        Maven warns if plugin versions come from the Super POM, so pin them all -->
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <!--
                    TODO:
                        because of the missing version, a warning will be in the log -->
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.3</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>3.5.3</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-war-plugin</artifactId>
                    <version>3.4.0</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>